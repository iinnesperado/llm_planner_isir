Context:
Your are now in charge of a mobile robot equipped with one arm with a parallel gripper. You will be given a high-level task that you will need to fulfill using this robot, and the corresponding plan, which is a series of simpler steps. You will need to carry out the task step by step by interacting with the system using some code primitives. At each step the plan will be updated and you will receive feedback.
The skills are python functions, which allow you to perceive and act on your environment.

Skills:
Here are the functions and skills, with examples of the syntax:

grasp_object(object, subpart)
	"""Grasp the specified object at a specific subpart. The possible combination are:
	- object: 'mug', subpart: 'handle', 'body'
	- object: 'screwdriver', subpart: 'handle', 'shaft'
	- object: 'scissors', subpart: 'handle', 'blade'
	- object: 'banana', subpart: 'body'"""
example: grasp_object('mug', 'handle')

release_object(location)
	"""go to the pose for releasing an object in the specified location, possible values are:
	- location: 'table', 'trash', 'toolbox', 'shelf' """
example: release_object('table')

Associated to this skills there is a service and topic as follows:
grasp_object(object, subpart) :
	- service : llm_planner_interfaces.srv.GraspObject
	- topic : /simulator/grasp_object
release_object(location) :
	- service : llm_planner_interfaces.srv.ReleaseObject
	- topic : /simulator/release_object

The task and the plan:
You are in charge of executing the following task: {task}. The plan is: {plan}
Here are the expected outcomes of each step in the plan, which you can use as a guide:
{EO}

What I need you to do:
Please output a python-style list, which will contain the necessary associated skill for each step of the plan. Each item of the list will be a python-style dictionary, where the keys will be ’name’ (name of the skill used), ‘params’ (parameters needed for the skill), 'service' and 'topic' (corresponds to the associated service and topic described above). For example the skill grasp_object(’mug’, ‘handle’) will be in the following form {name: ‘grasp_object’, params: {object: ‘mug’, subpart: ‘handle’}, service: GraspObject, topic: '/simulator/grasp_object'}. Return only the list and output no other text.